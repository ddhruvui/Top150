# Prediction stack (blueprint §0 code targets). Data fetchers stay stdlib-only.
pandas>=2.2
pyarrow>=15
numpy>=1.26
PyYAML>=6
scipy>=1.12
lightgbm>=4.3
scikit-learn>=1.4
optuna>=3.6
vectorbt>=0.26          # M15 fast-path cross-check engine
torch>=2.2              # M7 GRU / M8 CNN (GPU on RunPod)
exchange_calendars>=4.5 # already used by fetch_calendar
pytest>=8
