# Build-time only (Vite inlines it). Leave empty for local dev — vite.config.js
# proxies /api to the backend on :8787. On Render, set it in the service's
# Environment tab to the deployed API origin, no trailing slash.
VITE_API_BASE=
# e.g. VITE_API_BASE=https://top150be.vercel.app
